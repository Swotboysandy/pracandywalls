import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pracandywalls.app',
  appName: 'PracandyWalls',
  webDir: 'dist/public'
};

export default config;
